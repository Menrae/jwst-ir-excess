# requirements.txt
#
# Pinned loosely for now (Phase 1 scaffold). Will tighten pins once the
# pipeline is functional and we know which versions were actually used
# to produce results (important for reproducibility in the writeup).

# Core scientific stack
numpy>=1.26
scipy>=1.11
astropy>=6.0
pandas>=2.1

# Data structures for the tsdat-inspired pipeline stages
xarray>=2024.2
netCDF4>=1.6

# Archive access
astroquery>=0.4.7   # MAST, Gaia, VizieR (2MASS) access

# Stellar photosphere models / synthetic photometry -- all smoke-tested
# live and now used by pipeline/photosphere.py (see RESEARCH_CONTEXT.md
# Decision Log, 2026-07-20 entries).
synphot>=1.3
stsynphot>=1.5   # Kurucz (ck04models) grid access, requires local CDBS files
expecto>=0.1     # PHOENIX (Husser et al. 2013) grid access
dustmaps>=1.0    # Bayestar19 3D dust map (extinction)
dust_extinction>=1.4  # G23 (Gordon et al. 2023) reddening law

# MIRI photometry extraction (pipeline/miri_photometry.py) -- model-PSF
# fitting + aperture cross-check, both corrected via the CRDS apcorr
# reference file. Smoke-tested live 2026-07-20/21, see RESEARCH_CONTEXT.md.
photutils>=3.0   # PSFPhotometry, ImagePSF, aperture photometry
stpsf>=2.2       # MIRI model PSF generation (renamed from webbpsf); auto-
                 # downloads its own ~129 MB reference data on first use

# Plotting
matplotlib>=3.8

# Config handling
pyyaml>=6.0

# Testing
pytest>=8.0

# Notebooks
jupyterlab>=4.0
ipykernel>=6.29

# LaTeX table export
# TODO: confirm final choice; astropy.table has built-in latex writer,
# so this may not be needed as a separate dependency.
